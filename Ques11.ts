/* Names:store the names of a few of your friends in a array called names. Print each person name by accesssing
each element in the list,once at a time.*/
let friendslist : string[]=["maheen","Ayra","Amna","Mubeen","Bilal"];
console.log(friendslist);